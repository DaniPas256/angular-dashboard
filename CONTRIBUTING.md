# Contributing

Thanks for your interest in contributing! Quick start:

```bash
npm install
npm start
npm test
npm run test:coverage
```

- Fork the repo and open a PR against `main`.
- Keep changes focused and add tests when possible.
- For substantial changes, open an issue first to discuss the approach.

Run tests locally with `npm test`. Generate coverage with `npm run test:coverage`.
